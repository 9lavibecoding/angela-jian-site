{
  "name": "angela-jian-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "node scripts/check-precommit.mjs",
    "check:revenue": "node scripts/check-revenue.mjs",
    "check:build": "node scripts/check-build.mjs",
    "check:live": "node scripts/check-live.mjs",
    "preflight": "npm run check && npm run build && npm run check:build && npm run check:revenue"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "@notionhq/client": "^2.3.0",
    "@supabase/supabase-js": "^2.99.3",
    "@tailwindcss/vite": "^4.2.1",
    "astro": "^6.0.2",
    "contentful": "^11.10.5",
    "tailwindcss": "^4.2.1"
  }
}
